---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
version: 1
name: atomic-niri-itterum-nvidia
description: Bazzite DX GNOME with Niri for desktop with nvidia.

base-image: ghcr.io/ublue-os/bazzite-dx-nvidia-gnome
image-version: stable

modules:
  - type: dnf
    repos:
      copr:
        - avengemedia/danklinux
        - avengemedia/dms
        - scottames/ghostty
    install:
      packages:
        - niri
        - dms
        - helix
        - zed
        - ghostty

  - type: dnf
    no-cache: true
    install:
      packages:
        - https://persistent.oaistatic.com/codex-app-prod/linux/rpm/latest/chatgpt.x86_64.rpm
  - type: default-flatpaks
    configurations:
      - notify: true
        scope: system
        install:
          - com.brave.Browser
          - org.telegram.desktop
          - com.github.tchx84.Flatseal

  - type: files
    files:
      - source: system
        destination: /

  - type: signing
